## 示例
```
if boolean-expression {
  //if true, to do
}

if boolean-expression {
  //if true, to do
} else {
  //if false, to do
}

@@ case分支可为任意相同类型值，不需要break
switch var{
  case var1:
    //to do
  case var2:
    //to do
  default:
    //to do
}
```
